{
  "name": "scrolly-js",
  "version": "0.1.0",
  "description": "The scrollytelling framework — you write the DOM, scrolly runs the state machine, effects live in your CSS.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sixthshift/scrolly-js.git"
  },
  "homepage": "https://github.com/sixthshift/scrolly-js#readme",
  "bugs": {
    "url": "https://github.com/sixthshift/scrolly-js/issues"
  },
  "main": "./dist/scrolly.esm.js",
  "types": "./dist/scrolly.d.ts",
  "exports": {
    ".": {
      "types": "./dist/scrolly.d.ts",
      "default": "./dist/scrolly.esm.js"
    },
    "./scrolly.css": "./dist/scrolly.css",
    "./dist/*": "./dist/*"
  },
  "files": [
    "dist",
    "src",
    "themes"
  ],
  "sideEffects": false,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "bun run build && playwright test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "bun run typecheck && bun run lint && bun run test && bun run test:e2e && node scripts/check-links.mjs && git diff --exit-code dist examples skill/assets"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@microsoft/api-extractor": "^7.58.9",
    "@playwright/test": "^1.61.1",
    "esbuild": "^0.28.1",
    "happy-dom": "^20.10.6",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vite-plugin-dts": "^5.0.3",
    "vitest": "^4.1.9"
  }
}
