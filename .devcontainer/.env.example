# Copy to .env (gitignored). Loaded into the dev container via env_file.
# Scrolly needs no secrets today — this exists so the compose env_file
# reference resolves and future dev-only credentials have a home.
