- [evidence: 1; last: scrolly-v05-dx@012768be/2026-08-08T04:35:03.361Z] A plain-object lookup table keyed by an author-supplied attribute string (e.g. `TABLE[name]`) resolves JS's inherited prototype keys (`constructor`, `toString`, `valueOf`, …) as valid entries, silently producing NaN/wrong output with no fallback and no warning — an enumerated-value acceptance check (testing only one or two named strings) will not catch this. Guard every such table with `Object.create(null)`/`__proto__: null` or a `hasOwn` check; an array-scan lookup (e.g. `array.indexOf`) is immune by construction and is the lower-risk default. — T007 (escaped, `SHOT_FRACTIONS[shot]` at src/camera.ts), repaired by T014
- [evidence: 1; last: scrolly-v05-dx@012768be/2026-08-08T04:35:03.361Z] A grep-based check over fixture/example HTML can false-positive or false-negative against `bun run build`'s own sync-embeds step, which writes a library-banner comment into every marker block of every generated fixture — the banner's text can satisfy a should-be-absent check or trip a should-be-clean one. Strip marker-block regions before grepping fixture markup. — T008 (seq115 typo-amendment, counter-proven on three genuinely figure-free fixtures)
- [evidence: 1; last: scrolly-v05-dx@012768be/2026-08-08T04:35:03.361Z] A grep-based "sentinel" asserting the absence of a literal token (e.g. `! grep -q 'data-scrub' src/story.ts`) checks a string, not the invariant it stands for — a later ticket's own prose/comment mentioning that token for an unrelated reason (e.g. naming what a diagnostic deliberately excludes) re-trips it even though the actual invariant (no scrub code in the file) still holds. Word such comments around the literal token, or check code shape instead of grepping for it. — regression introduced by T008, repaired by T015 (seq177, seq183)
- [evidence: 1; last: scrolly-v05-dx@012768be/2026-08-08T04:35:03.361Z] Gzip-size headroom against a frozen byte bar (test/unit/size.test.ts) drifts down with every merged ticket; a headroom figure written once into ticket context goes stale within 1-2 closes, and left uncorrected can park several otherwise-unrelated tickets simultaneously on a premise nobody re-measured (three tickets parked within five minutes against a stated 444 B headroom when the real figure was 12 B). Re-measure committed dist/ bytes fresh at dispatch time rather than trusting a copied number; correct the field in place rather than appending another addendum, which just adds a new stale copy to overlook next time. — T005, T006, T008 (seq49/51/53 parks; seq105 shows the same stale figure recurring even after two prior corrections)
- [evidence: 1; last: scrolly-v05-dx@012768be/2026-08-08T04:35:03.361Z] This coordinator's `loop prompt <role>` rendering intermittently truncates at exactly 8192 bytes when stdout is a pipe (observed on a review render and on bare pipeline renders). Always redirect to a file (`loop prompt role --vars - < vars.json > prompt.md`) and verify the tail contains the closing section before dispatching; never trust a piped render. — seq28 (T002 review render)
